"""learning — ko'rinmas SRS dvigateli (SPEC §4) — platformaning yuragi.

Skeleton: hozircha model yo'q.

Faza 6'da qo'shiladi: ChildWordState, ChildLetterState (Leitner→FSRS-lite),
LearningEvent (har o'zaro ta'sir), SessionLog. SRS xizmati: record_result,
get_due, get_session_queue (due+yangi interleave, semantik interferensiyasiz).
"""
