"""billing — obuna va B2B litsenziya (SPEC §10, TALIM `ModuleLicense` g'oyasi).

Skeleton: hozircha model yo'q.

Kelajakda qo'shiladi: Subscription, Institution(branding_config_json),
ModuleLicense(institution→, module_key, signed_key, valid_until),
BrandingConfig (scope[global|institution]) — `/api/config/` shu yerdan.
"""
